<Window x:Class="DevKit.Views.NewGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Group"
        Width="380" Height="190"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource BgDeep}"
        Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/DevKit;component/Helpers/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Group name:"
                   Foreground="{DynamicResource TextSecondary}"
                   FontSize="13"
                   Margin="0,0,0,8"/>

        <TextBox x:Name="txtGroupName"
                 Grid.Row="1"
                 Background="{DynamicResource BgInput}"
                 Foreground="{DynamicResource TextPrimary}"
                 BorderBrush="{DynamicResource Border}"
                 BorderThickness="1"
                 FontSize="14"
                 Padding="8,6"
                 KeyDown="TxtGroupName_KeyDown"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Style="{DynamicResource BtnSm}"
                    Background="{DynamicResource BgCard}"
                    Foreground="{DynamicResource TextPrimary}"
                    Padding="20,6"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="BtnCancel_Click"/>
            <Button Content="Create"
                    Style="{DynamicResource BtnSm}"
                    Background="{DynamicResource AccentGreen}"
                    Foreground="#1E1E2E"
                    FontWeight="SemiBold"
                    Padding="20,6"
                    IsDefault="True"
                    Click="BtnCreate_Click"/>
        </StackPanel>
    </Grid>
</Window>
